package com.example.bullet.ui.theme

import androidx.compose.ui.graphics.Color

// ── Light — warm cream / ink ──────────────────────────────────────────────────
val Cream           = Color(0xFFFFFCED)   // page background (#fffced from Figma)
val CreamSurface    = Color(0xFFECE9D8)   // headers / bottom nav (#ece9d8 from Figma)
val CreamVariant    = Color(0xFFE4E1D0)   // slightly recessed surfaces
val Ink             = Color(0xFF1D1B20)   // primary — buttons, icons, filled circles
val InkSoft         = Color(0xFF6B6560)   // secondary text
val InkDivider      = Color(0xFFD4CFC6)   // subtle row dividers
val InkBorder       = Color(0xFF1D1B20)   // sharp outlines

// ── Dark — charcoal / paper ───────────────────────────────────────────────────
val Charcoal        = Color(0xFF141414)   // page background
val CharcoalSurface = Color(0xFF1E1E1E)   // cards / sheets
val CharcoalVariant = Color(0xFF272727)   // slightly recessed surfaces
val Paper           = Color(0xFFECE9D8)   // primary — cream on dark
val PaperSoft       = Color(0xFF9A9590)   // secondary text
val PaperDivider    = Color(0xFF2E2E2E)   // subtle row dividers
val PaperBorder     = Color(0xFF3A3A3A)   // soft outlines
